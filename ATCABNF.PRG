SET TALK OFF
CLEAR
CLOSE DATABASES

SELECT 0
SELECT CGC,NNF,DEMI;
  FROM CADNF;
  GROUP BY NNF;
  ORDER BY NNF;
  INTO TABLE WORK
INDEX ON NNF TO WORK1

SELECT 0
USE CABECNF INDEX CABECNF
SET RELATION TO NNF INTO WORK                

GO TOP             
DO WHILE ! EOF()
  REPLACE CGC      WITH WORK->CGC
  REPLACE DEMISSAO WITH CTOD(WORK->DEMI) 
  SKIP
ENDDO
CLOSE DATABASES
SET TALK ON





